```markdown
# Pet-проект на Golang

![Схема проекта](image.png)

## Техническое задание

Проект представляет из себя приложение **«Чек-Лист»**. С помощью него вы можете создавать, получать, удалять и отмечать как выполненные свои задачи.  
Ну, все же знают приложение «Чек-Лист», наподобие Microsoft ToDo? 😁

Схема проекта дана на изображении выше. То, что отмечено пунктирной линией, **не является обязательным** к выполнению, но позволит вам более уверенно отвечать на собеседованиях и комфортнее чувствовать себя на первом рабочем месте.

---

## API-сервис

**API-сервис** — это микросервис, который принимает HTTP-запросы от пользователя.

Поддерживаемые запросы:

1. **`POST /create`**  
   Создать задачу.  
   В теле запроса можно передать:
   - заголовок задачи;
   - содержание задачи.

2. **`GET /list`**  
   Получить список задач.  
   В теле ответа нужно вернуть:
   - заголовок;
   - содержание;
   - уникальный идентификатор каждой созданной задачи.

3. **`DELETE /delete`**  
   Удалить задачу.  
   В теле запроса необходимо указать **уникальный идентификатор удаляемой задачи**, чтобы вообще понимать, что удалять.

4. **`PUT /done`**  
   Отметить задачу выполненной.  
   В теле запроса необходимо передать **уникальный идентификатор выполненной задачи**.

> Всё, что делает API-сервис — это проксирует пользовательские запросы в БД-сервис  
> и опционально отправляет сообщения в Kafka-сервис (что за сообщения — описано ниже).

---

## БД-сервис

**БД-сервис** принимает запросы от API-сервиса и непосредственно работает с базами данных.

Варианты взаимодействия:

- упрощённый вариант — запросы по **HTTP**;
- более продвинутый вариант — запросы по **gRPC**.

Принимая запрос от API-сервиса, БД-сервис понимает, что от него требуется, и делает одно из следующих действий:

1. **Создание задачи**  
   - создаёт и сохраняет в PostgreSQL новую задачу, информацию о которой ему передал API-сервис;
   - присваивает задаче **уникальный идентификатор**, чтобы в будущем можно было обращаться к конкретной задаче;
   - добавляет к задаче информацию о том, выполнена ли она (чтобы мы могли видеть, какие задачи уже *done*, а какие — нет).

2. **Получение списка задач**  
   - достаёт из PostgreSQL полный список задач:  
     уникальный идентификатор, заголовок, описание и информацию о том, выполнена ли задача;
   - отдаёт этот список в ответе API-сервису.

3. **Удаление задачи**  
   - удаляет по уникальному идентификатору созданную ранее задачу из PostgreSQL.

4. **Обновление статуса задачи**  
   - переводит созданную ранее задачу в состояние **«Выполнено»**.

### Опционально: кеширование в Redis

БД-сервис может кешировать в **Redis** задачи, с которыми недавно взаимодействовал пользователь.

- В Redis необходимо настроить **TTL** на каждый ключ.
- Тема кеширования не самая простая, и в рамках задания подробно не расписывается.
- Кеширование задач и работа с Redis вынесены в секцию *«Опционально»* и рекомендуется к самостоятельной проработке оптимального подхода.

---

## Kafka-сервис (опционально)

**Kafka-сервис** полностью опционален, но, как и все остальные опциональные пункты, **рекомендуется к выполнению**.

Он представляет собой обычный воркер, который:

- слушает топик сообщений от API-сервиса;
- обрабатывает события и логирует их.

### Поведение API-сервиса

При каждом действии пользователя (создание, удаление, обновление задачи, получение списка задач), **после** проксирования запроса в БД-сервис, API-сервис _опционально_ отправляет в Kafka информацию об этом событии:

1. время, когда пользователь отправил свой запрос;
2. действие, которое сделал пользователь  
   (создал, удалил, обновил, получил задачу).

### Поведение Kafka-сервиса

Kafka-сервис в это время:

- вычитывает из Kafka все сообщения от API-сервиса;
- логирует их в файл.

---

## Дополнительные требования

- В проекте должны присутствовать **юнит-тесты**, и опционально — **интеграционные тесты**.
- Все микросервисы, базы данных и Kafka должны быть запущены в **Docker-контейнерах** при помощи **Docker Compose**.
- Опционально в проект можно добавить **линтер**.
- Запуск проекта, запуск тестов, запуск линтера должны производиться **одной командой** при помощи **Makefile**.
- Каждый сервис должен:
  - логировать входящие запросы;
  - логировать исходящие ответы;
  - логировать возникающие во время работы ошибки.  
  Логирование должно происходить **в файл**, чтобы по содержимому логов можно было понять, что происходило в системе.
- Работа над проектом должна вестись в рамках любого **Git-репозитория** (например, GitHub).  
  Он бесплатный и отлично подходит для некоммерческой разработки.

---

## Заключение

Для опытного человека реализация данного пет-проекта занимает **пару дней**.  
Но для человека без опыта этот проект может занять **от нескольких недель и более**.

После выполнения данного пет-проекта вы сможете смело заявлять на собеседовании, что:

- имеете опыт работы с перечисленными технологиями;
- и не боитесь какой-нибудь «хрени» — будь то Makefile, линтер или запрос в базу данных
```
